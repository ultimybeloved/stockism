# Sandbox mode flag. Loaded only when Vite runs with `--mode emulator`
# (via `npm run dev:emulator`). Points the app at the local Firebase emulators
# instead of production. Safe to commit — contains no secrets.
VITE_USE_EMULATOR=true
